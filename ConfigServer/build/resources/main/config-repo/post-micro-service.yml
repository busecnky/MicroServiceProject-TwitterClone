server:
  port: 6160

spring:
  zipkin:
    enabled: true
    base-url: http://localhost:9411
    service:
      name: driver-service
  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: ${RB}

  data:
    mongodb:
      host: localhost   #localhost
      port: 27000    # buse:29000 , furkan:27000
      username: ${MONGOPAWERPOSTUSERNAME}
      password: ${MONGOPAWERPOSTPASSWORD}
      database: PostDB